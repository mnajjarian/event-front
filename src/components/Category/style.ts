import styled from 'styled-components'

const Category = styled.article`
  padding: 3.5em 1em;
  margin-bottom: 0.25em;
  background: transparent;
  overflow: hidden;
`

const Title = styled.h2`
  margin-top: 0;
  font-size: 2em;
  color: blueviolet;
`

export const Styled = { Category, Title }
